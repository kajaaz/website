{
  "name": "Prolog",
  "year": 1972,
  "paradigm": "logic-relational",
  "typeSystem": "Dynamically typed terms with no static type checking; unification operates on tagged terms such as atoms, numbers, variables, and compound terms.",
  "runtime": "Typically compiled to implementation-specific virtual-machine instructions; some systems also compile to native code",
  "runtimes": [
    { "name": "SWI-Prolog VM", "kind": "bytecode-vm", "role": "Compiles clauses to SWI-Prolog's compact ZIP-derived virtual-machine instruction set, which is designed to retain source-level debugging support." },
    { "name": "SICStus Prolog VM", "kind": "bytecode-vm", "role": "Compiles Prolog to implementation-specific abstract-machine instructions in the WAM tradition." },
    { "name": "GNU Prolog (native)", "kind": "native", "role": "Compiles through WAM code and mini-assembly to standalone native executables." }
  ],
  "compilers": [
    { "name": "SWI-Prolog", "role": "Popular implementation with a virtual-machine compiler and rich libraries." },
    { "name": "GNU Prolog", "role": "Compiles to native code." }
  ],
  "concurrencyModel": "Implementation-dependent; SWI-Prolog supports native threads with message queues",
  "codeExample": {
    "lang": "prolog",
    "source": "parent(tom, bob).\nparent(tom, liz).\nparent(bob, ann).\n\nancestor(X, Y) :- parent(X, Y).\nancestor(X, Y) :- parent(X, Z), ancestor(Z, Y).\n\n?- ancestor(tom, ann)."
  }
}
